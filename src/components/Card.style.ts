import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    categoryView:{
        marginBottom: 2,
        padding: 10,
        borderWidth: 1,
        borderColor: 'grey',
        flex: 1,
        justifyContent: 'space-between'
    },
    categoryName: {
        fontSize: 18,
    }
});